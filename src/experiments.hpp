#ifndef _WEEDSIMEXP
#define _WEEDSIMEXP

#include "weedsim.hpp"
#include <fstream>




extern const std::string EXPERIMENTS_DESCRIPTION; 

void experiment_0(WeedSimulator * sim);

void experiment_1(WeedSimulator * sim);

void experiment_2(WeedSimulator * sim);

#endif